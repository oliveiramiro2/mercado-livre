import React from "react";

const Home: React.FC = () => {
    return (
        <div className="flex w-screen h-screen items-center justify-center">
            Home page
        </div>
    );
};

export default Home;
